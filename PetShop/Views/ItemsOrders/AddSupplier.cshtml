@model PetShop.Models.ViewModels.OrderViewModel

<div class="modal-content">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h4>Supplier</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <p>@String.Join(" ", Model.SupplierErrorMessages)</p>
            <div class="form-group">
                <label for="phoneNumber">Customer</label>
                <input list="customers" name="phoneNumber" id="customer" />
                <datalist id="customers" style="max-height: 150px">
                    @foreach (var supplier in Model.Suppliers)
                    {
                        <option value=@supplier.PhoneNumber>@String.Format("{0} {1}", supplier.Name, supplier.Surname)</option>
                    }
                </datalist>
            </div>

            <div class="form-group">
                <div>
                    @Html.ActionLink("Cancel", "Create")
                </div>

                <div>
                    @Html.ActionLink("Register", "RegisterSupplier", "Purchases")
                </div>

                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Create" class="btn btn-default" id="submitButton" />
                </div>
            </div>
        </div>
    }
</div>



@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

